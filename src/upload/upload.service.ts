// Upload Service
// 비즈니스 로직 계층. 파일 형식/크기 검증 후 Supabase Storage 업로드·공개 URL 반환 담당.
// DB 테이블이 아닌 스토리지 대상이므로 repository 계층은 두지 않는다.
// TODO: multer + Supabase Storage 연동으로 실제 업로드 구현.

export const uploadService = {};
