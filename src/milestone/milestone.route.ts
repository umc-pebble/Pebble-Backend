import { Router } from 'express';

const router = Router();

// GET    /api/v1/milestones?categoryId=
// POST   /api/v1/milestones
// PATCH  /api/v1/milestones/:id
// DELETE /api/v1/milestones/:id

export default router;
